(menhir (modules parser))
(ocamllex lexer)
( library
  ( name
     trio
  )
  ( public_name
     trio
  )
  (preprocess (pps ppx_deriving.std ppx_compare))
  (libraries batteries)
  ( flags (:standard -warn-error -A))
)
